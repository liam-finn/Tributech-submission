apiVersion: v1
kind: Service
metadata:
  name: {{ include "tributech-assignment-chart.fullname" . }}
  labels:
    {{- include "tributech-assignment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tributech-assignment-chart.selectorLabels" . | nindent 4 }}
